import 'package:flutter_test/flutter_test.dart';

void main() {
  // Benchmarks don't define tests but need to be run with `flutter test`.
  // If no tests are discovered `flutter test` fails. This test prevents that.
  test('stub', () {});
}
